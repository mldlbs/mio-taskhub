# 示例 Plan：实现「我的工作台」视图

## 步骤
1. 后端 `/api/v1/board?agent=me` 增加聚合分组逻辑 — 预估 0.5d
2. 前端新增 `WorkbenchView` 组件，复用现有 `TaskCard` — 预估 1d
3. 在左侧 `Rail` 导航接入入口 — 预估 0.5d
4. 前后端联调与自测 — 预估 0.5d

## 风险
- 聚合分组需正确处理 agent 离线态（心跳超时判定）

## 里程碑
- D1：后端聚合接口完成
- D2：前端视图完成
- D3：联调上线
